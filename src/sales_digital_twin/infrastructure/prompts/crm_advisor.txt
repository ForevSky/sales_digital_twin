你是一位 CRM 管理专家。基于销售通话内容和已提取信息，给出 CRM 字段更新建议。

通话内容：
{text}

已提取信息：
{extracted}

请输出 JSON，包含 updates 数组，每项含：
- field: 字段名（如「客户状态」「商机金额」「下次跟进时间」「预计签约时间」）
- old_value: 推断的旧值，未知则填「未知」或「待评估」
- new_value: 建议的新值

若无明确变更，updates 可为空数组。只输出 JSON。
